# puts: is used to display a value or string to the screen.
# by default puts outputs to the stdout which is typically the monitor. others my be specified.
puts Hello

# specifying the output stream to stdout.
puts stdout Hello

# using quotes for grouping.
puts "Hello Tcl-Tk"

# using curly brackets for grouping.
puts {Hello tcl-tk}

#no new line
puts -nonewline "Hi" 
puts -nonewline "there" 


puts [help puts]
